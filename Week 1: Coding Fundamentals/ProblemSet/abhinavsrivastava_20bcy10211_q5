#python
try:
    t = int(input())
    for i in range(t):
        n, m, k = map(int, input().split())
        a = abs(n - m)
        if (a <= k):
            print(0)
        else:
            print(a - k)
except:
    pass
