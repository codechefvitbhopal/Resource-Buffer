#include <iostream>
#include <string>
using namespace std;

int main()
{ string s,s1;
getline(cin,s);
int x=0;
while(s[x]!=' '){
 s1+=s[x];
 x++;
}
s.erase(0,s1.length()-1);
 s1 = string(s1.rbegin(),s1.rend()); 
cout<<s1<<s;
    return 0;
}
