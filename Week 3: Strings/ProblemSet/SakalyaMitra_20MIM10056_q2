def encryp(s, k) :
    if not s.isalpha() :
        return s
    if s.islower() :
        t = 97
    else :
        t = 65
    return chr(t + ((ord(s)+k-t)%26))


n = int(input())
s = input()
k = int(input())
d = ''
for i in s :
    d = d + encryp(i, k)
print(d)    
